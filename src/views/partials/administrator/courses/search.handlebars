<div class="table-responsive">
    <table class="table table-hover" id="basicTable">
        <thead>
        <tr>
            <th>Curso</th>
            <th>Tipo/Categoria</th>
            <th>Módulos</th>
            <th>Status</th>
            <th>Última Atualização</th>
            <th></th>
        </tr>
        </thead>
        <tbody>

		<?php

		use App\Entity\School\Course\Course;

		if (count($result) > 0) {
			foreach ($result as $r) { ?>

                <tr>
                    <td class="v-align-middle ">
                        <p><?php echo mb_strtoupper($r->getCourse()); ?></p>
                    </td>
                    <td class="v-align-middle">
                        <p><?php echo $r->getType()->getFullNameType(); ?></p>
                        <small><?php echo $r->getCategory()->getCategory(); ?></small>
                    </td>
                    <td class="v-align-middle">
                        <p><?php echo count($r->getModules()); ?></p>
                    </td>
                    <td class="v-align-middle">
                        <p>
							<?php

							if ($r->getStatus() == Course::COURSE_ACTIVE)
								echo 'Ativo';

							if ($r->getStatus() == Course::COURSE_INACTIVE)
								echo 'Inativo';

							if ($r->getStatus() == Course::COURSE_CANCELED)
								echo 'Cancelado';

							?>
                        </p>
                    </td>
                    <td class="v-align-middle">
                        <p><?php echo $r->getUpdatedAt()->format('d/m/Y'); ?></p>
                    </td>
                    <td class="v-align-middle">
                        <a href="<?php echo $route->pathFor('administrator.courses.view', ['hash' => $r->getHash()]); ?> " class="btn btn-secondary w-100 delete">
                            <span class="p-t-5 p-b-5 "> <i class="fa fa-edit fs-15"></i> </span> <br>
                            <span class="fs-11 font-montserrat text-uppercase">Abrir Curso</span>
                        </a>
                    </td>
                </tr>

			<?php }
		} else { ?>

            <tr>
                <td colspan="6">
                    <h4 class="text-danger text-center"> Nenhum registro encontrado</h4>
                </td>
            </tr>

		<?php } ?>

        </tbody>
    </table>
</div>
