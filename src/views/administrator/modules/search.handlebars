<div class="table-responsive">
    <table class="table table-hover" id="basicTable">
        <thead>
        <tr>
            <th>Módulo</th>
            <th>Cursos</th>
            <th>Núcleo Comum (Tipos)</th>
            <th>Status</th>
            <th>Última Atualização</th>
            <th></th>
        </tr>
        </thead>
        <tbody>

		<?php

		use App\Entity\School\Course\Course;

		if (count($result) > 0) {
			foreach ($result as $r) { ?>

                <tr>
                    <td class="v-align-middle ">
                        <p class="text-uppercase"><?php echo $r->getModule(); ?></p>
                    </td>
                    <td class="v-align-middle">
                        <p><?php echo count($r->getCourse()); ?></p>
                    </td>
                    <td class="v-align-middle">
                        <p><?php echo count($r->getType()); ?></p>
                    </td>
                    <td class="v-align-middle">
                        <p><?php echo $r->showStatus(); ?></p>
                    </td>
                    <td class="v-align-middle">
                        <p><?php echo $r->getUpdatedAt()->format('d/m/Y'); ?></p>
                    </td>
                    <td class="v-align-middle">
                        <a href="<?php echo $route->pathFor('administrator.modules.record', ['hash' => $r->getHash()]); ?> " class="btn btn-secondary w-100 delete">
                            <span class="p-t-5 p-b-5 "> <i class="fa fa-edit fs-15"></i> </span> <br>
                            <span class="fs-11 font-montserrat text-uppercase">Abrir Módulo</span>
                        </a>
                    </td>
                </tr>

			<?php }
		} else { ?>

            <tr>
                <td colspan="6">
                    <h4 class="text-danger text-center"> Nenhum registro encontrado</h4>
                </td>
            </tr>

		<?php } ?>

        </tbody>
    </table>
</div>
